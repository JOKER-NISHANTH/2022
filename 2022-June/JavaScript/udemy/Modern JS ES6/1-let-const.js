let num = 10;
console.log(num);

let name = "Mr Black"
console.log(name)

const PI = 3.14
console.log(PI)
//! let and const not support Hoisting
console.log(hoist);
var hoist = "The process whereby the interpreter appears to move the declaration of functions, variables or classes to the top of their scope, prior to execution of the code."
console.log(hoist);

console.log(hoistLet);
let hoistLet = "The process whereby the interpreter appears to move the declaration of functions, variables or classes to the top of their scope, prior to execution of the code."
console.log(hoistLet);
